#set($delegateSimpleName = $tool.getSimpleName($delegateClass))
#set($delegateVariable = $tool.getVariableName($delegateClass))
$!{copyright}
package ${packageName};

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.stereotype.Component;
import javax.annotation.Resource;

/**
* @Description ${classDesc}
* @author $!{author}
* @date $!{tool.getDateTimeStr()}
*/
@Component
public class ${className} {

    private final static Logger log = LoggerFactory.getLogger(${className}.class);

    @Resource
    private $delegateSimpleName $delegateVariable;

}